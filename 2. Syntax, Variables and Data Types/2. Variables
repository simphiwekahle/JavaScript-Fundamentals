// Assigning variables values
let firstName = "Sphephelo";
let lastName = "Ndimande";
let numYearsEmployment = 2;
let dateBirth = new Date("July 5, 1997");

console.log(`${firstName} has been employed for ${numYearsEmployment} years.`);

// Changing a variable created with let
numYearsEmployment = 3;

// Using const to create a constant
const jobTitle = "Software Development Engineer";

console.log(`${jobTitle}`);